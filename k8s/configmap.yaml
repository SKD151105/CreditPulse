apiVersion: v1
kind: ConfigMap
metadata:
  name: creditpulse-config
  labels:
    app: creditpulse
data:
  NODE_ENV: "production"
  PORT: "5000"
  REDIS_URI: "redis://redis-service:6379"
  CLIENT_URL: "https://credit-pulse-xi.vercel.app"
  AWS_REGION: "ap-south-1"
  S3_BUCKET_NAME: "creditpulse-docs-skd"
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
